const state = {
  cardId: undefined
}

const getters = {}

const mutations = {
  setCardId(state, cardId){
    state.cardId = cardId;
  }
}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}

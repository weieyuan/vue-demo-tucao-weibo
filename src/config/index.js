export const cardConfig = {
  DEFAULT_PIC_URL: "static/images/person_default.png",
  DEFAULT_NAME: "匿***名"
};

const baseUrl = "tucao"
const globalConfig = {
  debug: true,
  baseUrl,
  endPoint: baseUrl + "/ws/tucao"
};

export {globalConfig};
